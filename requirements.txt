# Core dependencies for both Vercel and Railway
# Database
psycopg2-binary==2.9.10

# WebSocket (for Railway trading bot)
websocket-client==1.6.4

# OKX API (for Railway trading bot)
python-okx==0.4.0

# Environment variables
python-dotenv==1.0.1

# Flask (only for local trading_web_viewer.py, not needed for Vercel)
# Flask==3.1.0
# Werkzeug==3.0.3
